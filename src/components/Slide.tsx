const Slide = ({ heading, paragraph }) => {
    return (
      <div>
      <h2 style={{ color: 'white' }}>{heading}</h2>
<p style={{ color: '#D0DBDE' }}>{paragraph}</p>

      </div>
    );
  };
  
  export default Slide;
  